server:
  port: 80
  
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/health
    username: root
    password: mother
  
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
      
  mail:    host: smtp.gmail.com
    port: 587
    username: sample2025nit
    password: xxxxxxxx
    properties:
     mail:
      smtp:
       starttls:
         enable: true             
